# 創業節稅試算｜GitHub Pages 版

## 檔案
- `index.html`：公開前台，不提供後台入口。
- `admin.html`：獨立後台；開啟時只顯示帳號、密碼、登入按鈕，登入後才顯示完整管理內容。
- `tax-config.json`：前台目前正式使用的參數。
- `annual-presets.json`：113、114、115 年度預設參數。

## GitHub Pages
1. 將本資料夾檔案上傳到 Repository 根目錄。
2. Repository → Settings → Pages → Deploy from a branch → `main` / `/(root)`。
3. 前台：`https://<帳號>.github.io/<repo>/`
4. 後台：`https://<帳號>.github.io/<repo>/admin.html`

## 後台登入
目前帳號：`admin`
目前密碼：`password`

此登入屬 GitHub Pages 純前端存取門檻，不等同伺服器端安全驗證；請勿在頁面原始碼中存放 GitHub Token 或其他高敏感憑證。

## 年度參數
後台會依所選民國年度讀取 `annual-presets.json` 對應參數，目前內建 113、114、115 年。
財政部參考來源名稱與按鈕連結會隨所選年度切換，畫面不直接顯示完整網址。

## 前台計算項目啟用
後台可從既有免稅額／特別扣除額項目勾選「啟用」。
啟用後，前台自動新增對應欄位或自動計算項目，並納入綜合所得稅試算。

## 儲存參數
GitHub Pages 無法由瀏覽器直接覆寫 Repository。
後台修改後請下載新的 `tax-config.json`，再回 GitHub Repository 覆蓋同名檔案。

## 版本號
版本採 `vYYYY.MM`，例如 `2026.09.v06-21`。


## GitHub 後台同步版

本版本的 `admin.html` 可直接透過 GitHub Contents API 更新 Repository 內的 `tax-config.json`。

預設：
- Owner：`cronos-2026`
- Repository：`cronos-2026/tax`
- Branch：`main`
- 參數檔：`tax-config.json`

使用方式：
1. 進入 `admin.html`，以既有後台帳密登入。
2. 修改年度參數或啟用項目。
3. 在「GitHub 同步更新前台」貼上 GitHub Fine-grained personal access token。
4. Token 建議只授權此 Repository，並將 Repository permissions → Contents 設為 `Read and write`。
5. 按「同步更新前台」。
6. 系統會直接更新 GitHub 的 `tax-config.json`；GitHub Pages 部署完成後，前台重新整理即可讀取新參數。

安全說明：
- Token 不會寫入 `tax-config.json`。
- 本版不會把 Token 硬編碼在 `admin.html`。
- Token 僅存在目前開啟的瀏覽器頁面記憶體中；關閉或重新整理頁面後需重新輸入。
- 因為 GitHub Pages 是公開靜態網站，請不要把長期高權限 Token 寫進 HTML。


## GitHub 根網址專用

此版本已設定為 GitHub Pages 使用者根網站：

- 前台：https://cronos-2026.github.io/
- 後台：https://cronos-2026.github.io/admin.html
- GitHub Owner：cronos-2026
- Repository：tax
- Branch：main
- 前台參數檔：tax-config.json

### Token 權限
Fine-grained personal access token 必須把 Repository access 設為：
`Only select repositories → cronos-2026.github.io`

Repository permissions：
`Contents → Read and write`

若 Token 仍只授權舊的 `cronos-2026-tax-calculator`，
後台同步時會出現 `Resource not accessible by personal access token`。

### 使用方式
1. 將本 ZIP 內 5 個檔案上傳到 `cronos-2026/tax` Repository 根目錄。
2. 開啟 `/admin.html` 登入。
3. 修改後台參數。
4. 貼入已授權 `cronos-2026/tax` 的 Fine-grained Token。
5. 按「同步更新前台」。
6. 系統會直接更新 GitHub 的 `tax-config.json`。
7. GitHub Pages 部署完成後，重新整理前台即可看到新設定。


## 本次優化（tax 專用同步版）

後台同步目標已固定：
- Owner：`cronos-2026`
- Repository：`tax`
- Branch：`main`
- 檔案：`tax-config.json`

後台新增：
- 「測試 GitHub Token」按鈕：先確認 Token 是否可讀取目標檔案。
- 「同步更新前台」按鈕：確認後直接寫入 `tax-config.json`。
- GitHub Owner / Repository / Branch / 檔案路徑改為唯讀，避免誤改。
- 常見 Token 錯誤會轉成中文提示。

Token 必須使用 Fine-grained personal access token：
`Only select repositories → cronos-2026/tax`
Repository permissions：
`Contents → Read and write`


# TAX FINAL 2026.09.v06-21 — 實際 GitHub Pages 部署設定

本完整版本已固定為目前實際網站：

- GitHub Repository：`cronos-2026/tax`
- Branch：`main`
- 前台：`https://cronos-2026.github.io/tax/`
- 後台：`https://cronos-2026.github.io/tax/admin.html`
- 後台同步檔案：`tax-config.json`

後台同步欄位已固定：
- Owner：`cronos-2026`
- Repository：`tax`
- Branch：`main`
- 路徑：`tax-config.json`

Fine-grained Token：
- Repository access：Only select repositories → `cronos-2026/tax`
- Repository permissions：Contents → Read and write

後台提供「測試 GitHub Token」與「同步更新前台」。


## 前台「歸零」按鈕
- 「↺ 歸零」：只將目前畫面的輸入數字與試算結果歸零，不刪除任何已儲存資料。
- 「🗑️ 清除儲存資料」：刪除全部已儲存資料，並將目前輸入值歸零。


## 扶養人數驗證
前台已新增以下限制，扶養人數必須大於或等於各欄填寫人數：
- 70歲以上免稅額
- 身心障礙特別扣除額
- 幼兒學前－第1名子女
- 長期照顧特別扣除額

若不符合，按「立即計算比較」時會停止計算並提示；欄位的最大值也會依扶養人數動態調整。


## 扶養親屬資格可同時勾選
前台已改為逐位扶養親屬勾選：
- 70歲以上
- 身心障礙
- 長期照顧

三個資格彼此獨立，同一位扶養親屬可以同時勾選 2 項或 3 項，不是互斥選項。
系統會分別計入高齡免稅額、身心障礙特別扣除額與長期照顧特別扣除額。


## 扶養資格簡化優化
前台改為最基本、易操作的試算方式：
- 70歲以上受扶養親屬人數 ≤ 扶養人數
- 身心障礙特別扣除資格人數 ≤ 扶養人數
- 幼兒學前－第1名符合資格子女人數 ≤ 扶養人數
- 長期照顧特別扣除資格人數 ≤ 扶養人數
- 各資格分別檢查，不互斥，也不將不同資格人數相加後再比較扶養人數。
- 同一受扶養親屬可能同時符合多種資格，因此不同資格人數可重疊。


## 扶養資格欄位上限鎖定
- 70歲以上、身心障礙、幼兒學前第1名、長期照顧等資格人數，各自不得超過扶養人數。
- 當資格人數已達目前允許上限時，該欄位會進入鎖定狀態，不能再增加。
- 若要增加該資格人數，必須先把「扶養人數」提高。
- 若扶養人數降低，超過新上限的資格人數會自動降到新上限。
- 不同資格仍彼此獨立、不互斥，也不相加比較。


## 列印 1～2 頁自動優化
A4直式列印，縮小邊界與留白，並依內容高度自動切換緊湊列印密度，目標盡量維持1～2頁。列印時隱藏操作按鈕，重要區塊盡量避免跨頁；列印後恢復正常畫面。


## 後台「前台計算項目啟用設定」全選功能
- 新增「全選」按鈕：一次勾選此區所有可啟用項目。
- 新增「取消全選」按鈕：一次取消此區所有可啟用項目。
- 不影響鎖定或不可編輯的核心項目。


## 版本
目前版本：`2026.09.v06-21`。前台與後台網頁皆顯示版本號；後續更新依 `YYYY.MM.vDD`，同日多版再加 `-1`、`-2`。


## 2026.09.v06-21 版面調整
- 移除前台上方／固定位置的版本膠囊。
- 前台頁尾原「© 2026 . GitHub Pages 網頁版」改為「© 2026.09.v06-21」。
- 版本號不再鎖定顯示於畫面上方，改放在網頁最下方頁尾。


## 2026.09.v06-21
修正前台：完整移除頁面上方固定的 `2026.09.v06-21` 綠色版本標籤，版本僅保留於最下方 footer：`© 2026.09.v06-21`。


## 2026.09.v06-21 簡潔視覺優化
- 前台版面改為更簡潔的卡片與留白配置。
- 統一背景、邊框、輸入框與標題視覺層級。
- 需注意事項使用暖黃色警示區塊加強辨識。
- 重要錯誤/高風險訊息保留紅色語意；一般正常狀態採綠色語意。
- 降低陰影與過多裝飾，提升專業感與可讀性。


## 2026.09.v06-21
- 「清除儲存資料」只刪除瀏覽器中已儲存的多組資料。
- 不再歸零目前畫面的輸入值，也不清除目前試算結果。
- 「↺ 歸零」維持獨立功能，專門負責將目前畫面數值歸零。


## 2026.09.v06-21
- 修正扶養資格提醒區塊中 `<strong>` 被當成文字顯示的問題。
- 「注意：」現在會正常粗體顯示，不再露出 HTML 標籤。
- 保留原本暖黃色提醒樣式，並加強「注意：」的視覺層級。


## 2026.09.v06-21 列印分頁修正
- 修正列印時第 1 頁只有標題與提醒、主要試算內容被整塊推到下一頁的問題。
- 原因為大型卡片套用了 `page-break-inside: avoid`。
- 現改為大型卡片與表格可自然跨頁，只保護表格列、標題等小單位不被切斷。
- 保留 A4 自動壓縮與 1～2 頁列印目標。


## 2026.09.v06-21 列印 1～2 頁強化
- 列印主內容固定改為「左側輸入／右側試算結果」雙欄報表版面。
- A4 直式邊界縮至 5～6 mm，並壓縮卡片、輸入欄位、表格列高與間距。
- 列印時隱藏操作按鈕與部分非必要輔助文字，只保留核心輸入、結果與重要提醒。
- 完整取消大型卡片不可跨頁的規則，避免再次產生大面積空白頁。
- 不再依螢幕頁面高度強制套用極小 zoom；改由專用列印排版直接控制在 1～2 頁。


## 2026.09.v06-21 智慧列印分頁
- 列印時優先嘗試將「基礎資料設定」與「稅額試算比較表」排在同一頁。
- 若內容量較多、不適合維持一頁，則自動切換兩頁模式。
- 兩頁模式以兩個主要抬頭作為分頁基準：
  1. 第 1 頁：基礎資料設定
  2. 第 2 頁：稅額試算比較表
- 第 2 頁會從「稅額試算比較表」標題開始，避免內容被切得零散。


## 2026.09.v06-21
- 修正「清除儲存資料」確認文字。
- 改為：「確定要刪除全部已儲存資料嗎？目前畫面的輸入數字及試算結果會保留。」
- 清除儲存資料只刪除已儲存內容，不歸零目前畫面。


## 2026.09.v06-21 按鈕排版
- 第一列：儲存目前資料／開啟儲存資料
- 第二列：歸零／清除儲存資料
- 第三列：列印本頁


## 2026.09.v06-21 版本編碼優化
- 版本格式統一為：`年.月.v日-次數`
- 本版版本號：`2026.09.v06-21`
- 年份前不再加 `v`
- 前台、後台、頁尾、README 與 ZIP 檔名同步使用相同格式


## 2026.09.v06-21 列印空白頁修正
- 修正列印最後多出一頁、只剩「以115年度稅率、免稅額及扣除額基礎計算」的問題。
- 該螢幕版底部說明在列印時隱藏，不再單獨形成尾頁。
- 移除列印時可能造成額外頁面的最小高度與尾端強制分頁。
- 保留原本一頁優先、必要時依「基礎資料設定／稅額試算比較表」分成兩頁的規則。


## 2026.09.v06-21 學前子女同欄優化
- 「幼兒學前－第1名符合資格子女」與「幼兒學前－第2名以上符合資格子女人數」整合為同一個「幼兒學前特別扣除」群組。
- 兩個輸入欄位固定在同一欄中上下排列，不再被其他項目拆開。
- 列印時也維持同一欄位群組，避免分散到不同區塊。


## 2026.09.v06-21 後台學前子女同欄
- 後台「免稅額／一般與特別扣除額」中，幼兒學前第1名與第2名以上整合為同一個「幼兒學前特別扣除」群組。
- 後台「前台計算項目啟用設定」也同步以同一群組名稱呈現，仍保留兩項各自的啟用開關。
- 修正該區說明文字與「全選／取消全選」按鈕的 HTML 結構。


## 2026.09.v06-21
- 取消前台上方標題區固定／黏住效果。
- 「創業節稅試算」標題區改為一般頁面內容，向下捲動時會一起離開畫面。
- 不再保留頂部固定空間。


## 2026.09.v06-21 後台上方標題區編輯
後台新增／整理「前台上方標題區編輯」：
- 主標題
- 副標題／說明文字
- 重點提醒文字
- 重點提醒留空時，前台黃色提醒區塊自動隱藏
- 可透過 GitHub 同步更新至前台


## 2026.09.v06-21 可取消上方標題
- 後台「前台上方標題區編輯」新增「顯示上方標題區」勾選項。
- 取消勾選後，前台完整隱藏主標題、副標題與重點提醒。
- 重新勾選後即可恢復顯示。
- 設定會寫入 `tax-config.json` 並可透過 GitHub 同步更新。


## 2026.09.v06-21 後台標題編輯顯示修正
- 修正「顯示上方標題區」checkbox 被全域 input 寬度撐滿，造成文字與編輯欄位看不到的問題。
- 後台現在會清楚顯示：
  - 顯示上方標題區
  - 主標題
  - 副標題／說明文字
  - 重點提醒文字
- checkbox 也改為正確使用 checked 狀態讀寫 `showHeaderSection`。


## 2026.09.v06-21 後台版本格式修正
- 後台「網站版本」統一顯示為 `2026.09.v06-21`。
- 年份前不加 `v`。
- 格式固定為：`年.月.v日-次數`。
- 前台、後台、tax-config.json、README、ZIP 檔名全部同步。


## 2026.09.v06-21 最上方欄位可編輯／取消
- 將原本網頁最上方的空白欄位獨立成「前台最上方欄位」。
- 後台可勾選「顯示最上方欄位」。
- 可輸入「最上方欄位文字」。
- 取消勾選後，整個最上方欄位完全隱藏，不再保留空白高度。
- 此設定與「前台上方標題區編輯」分開，兩者可獨立控制。


## 2026.09.v06-21 移除頂部空白
- 完整移除網址列下方、主標題上方原本保留的空白欄位。
- 「創業節稅試算」標題區會直接往上貼近網頁頂端。
- 後台移除「前台最上方欄位編輯」設定。
- 後台保留「前台上方標題區編輯」，仍可控制主標題、副標題、重點提醒與是否顯示標題區。


## 2026.09.v06-22 教育學費與70歲以上不可重複
- 「教育學費特別扣除資格人數」與「70歲以上受扶養親屬人數」視為不可重複。
- 兩項合計不得超過扶養人數。
- 任一欄增加後，另一欄的可輸入上限會自動縮小。
- 若已達上限，欄位會鎖定；提高扶養人數後才可再增加。
- 身心障礙、長照等可重疊資格仍維持原本邏輯。


## 2026.09.v06-23 幼兒學前人數上限
- 「幼兒學前－第1名」與「第2名以上」符合資格子女人數合計不得超過扶養人數。
- 任一欄增加後，另一欄的可輸入上限會自動縮小。
- 達到扶養人數上限時欄位會鎖定；增加扶養人數後才可再增加。


## 2026.09.v06-24 特別扣除額條件重整
本版依特別扣除額資格重新分類：
- 70歲以上欄位明確限定為「受扶養直系尊親屬」。
- 70歲以上直系尊親屬＋教育學費子女＋幼兒學前子女，三類合計不得超過扶養人數。
- 身心障礙與長照資格可與其他資格重疊，不加入前述互斥加總。
- 教育學費改為「符合資格子女人數＋實際可扣除教育學費」，每人25,000元為上限，不足上限者按實際發生數。
- 長照與房租欄位增加提示：仍須另確認法定適用／排除條件，本工具不以人數邏輯代替資格判斷。


## 2026.09.v06-25 幼兒學前單一欄位
- 前台「幼兒學前特別扣除」改為單一「符合資格子女總人數」欄位。
- 計算方式：
  - 0 人：0 元
  - 1 人：第1名子女扣除額
  - 2 人以上：第1名扣除額 +（總人數－1）× 第2名以上每人扣除額
- 不再讓使用者分別輸入第1名與第2名以上人數。
- 幼兒總人數仍受扶養人數上限限制，並與70歲以上直系尊親屬、教育學費子女共同納入互斥群組。
- 後台仍保留兩個金額參數，因第1名與第2名以上適用金額不同；前台啟用則視為同一項功能。


## 2026.09.v06-26 權益比例版面修正
- 後台未啟用任何額外免稅額／特別扣除額時，前台不再保留空白區塊。
- 「商號組織型態」與「本人持股／盈餘分配比例」在桌面版改為同一列顯示，避免持股比例單獨形成一整欄。
- 手機版仍自動改為上下排列，維持可讀性。


## 2026.09.v06-27 智慧一頁或標題分二頁
- 列印時先嘗試將內容壓在一頁內，前提是版面仍可閱讀。
- 若內容無法合理放入一頁，則自動切換為兩頁列印。
- 第一頁以「基礎資料設定」為主。
- 第二頁強制從「稅額試算比較表」標題開始。
- 避免大區塊的 `page-break-inside: avoid` 造成不必要的空白頁。


## 2026.09.v06-28 列印基礎資料雙欄
- 列印時「基礎資料設定」改為左右兩欄排列。
- 兩欄內容維持在同一頁內，減少基礎資料被拉長造成分頁。
- 一頁模式與兩頁模式都套用雙欄。
- 若整份內容仍無法合理放在一頁，第二頁仍固定從「稅額試算比較表」開始。


## 2026.09.v06-29 超過二頁自動雙欄縮距
- 正常情況不強制「基礎資料設定」雙欄。
- 若正常列印預估會超過 2 頁，才將「基礎資料設定」改成左右雙欄。
- 啟用雙欄時會自動縮小欄距、上下間距、輸入框高度與說明文字行距。
- 若第一級壓縮後仍偏高，會再自動啟用第二級緊湊模式。
- 目標是將「基礎資料設定」完整壓在第一頁，第二頁從「稅額試算比較表」開始。


## 2026.09.v06-31 一頁優先，超出則雙欄分二頁
列印規則調整為：
- 「基礎資料設定」與「稅額試算比較表」基本上允許放在同一頁。
- 若整份內容可合理容納於一頁，維持一頁，不強制換頁。
- 若判斷會超出一頁：
  1. 「基礎資料設定」改為左右雙欄。
  2. 自動縮小欄距、上下間距、輸入框高度與說明文字行距。
  3. 若仍偏高，再啟用第二級緊湊模式。
  4. 強制將「基礎資料設定」完整放在第一頁。
  5. 第二頁從「稅額試算比較表」標題開始。


## 2026.09.v06-31 修復「立即計算比較」
- 修復前一版列印事件初始化順序造成的 JavaScript 執行中斷。
- 原先在 `adaptiveSmartPrint` 尚未建立前就直接綁定列印按鈕，瀏覽器會產生 `ReferenceError`。
- 因程式在該處中斷，後方「立即計算比較」按鈕事件未完成綁定，因此看起來完全不會計算。
- 改為點擊列印按鈕時才從 `window.adaptiveSmartPrint` 呼叫列印函式，不再阻斷頁面初始化。
- 本次不改變既有稅務計算公式與列印規則。
